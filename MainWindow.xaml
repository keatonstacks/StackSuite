<Window x:Class="StackSuite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StackSuite"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="StackSuite" Height="637" Width="1406"
        Style="{StaticResource BorderlessMaterialWindow}"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Icon="/StackSuiteImage.png">

    <!-- Card-like Border for the whole window -->
    <Border CornerRadius="12" Margin="0" Background="{DynamicResource MaterialDesignPaper}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.4"/>
        </Border.Effect>

        <DockPanel>
            <!-- Custom Title Bar -->
            <Border DockPanel.Dock="Top" Height="48" Background="{DynamicResource PrimaryHueDarkBrush}" MouseLeftButtonDown="CustomTitleBar_MouseLeftButtonDown">
                <Grid Margin="8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/StackSuiteImage.png" Height="40" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" Margin="0,0,8,0"/>
                    <TextBlock Grid.Column="1" Text="StackSuite" FontWeight="Bold" FontSize="18" Foreground="White" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Width="36" Height="36" Margin="0,0,4,0" Click="Minimize_Click" Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="WindowMinimize" Width="20" Height="20"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Width="36" Height="36" Margin="0,0,4,0" Click="Maximize_Click" Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="WindowMaximize" Width="20" Height="20"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Width="36" Height="36" Click="Close_Click" Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="WindowClose" Width="20" Height="20"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <ProgressBar
              DockPanel.Dock="Bottom"
              x:Name="ScanProgressBar"
              Height="4"
              Margin="0"
              IsIndeterminate="True"
              Visibility="Collapsed"
              Style="{StaticResource MaterialDesignLinearProgressBar}" />
            <!-- Main Content -->
            <TabControl x:Name="MainTabControl" Background="Transparent" FontFamily="Segoe UI" FontSize="15" Foreground="White">
                <!-- Home Tab -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Home" Width="20" Height="20" Margin="0,0,4,0"/>
                            <TextBlock Text="Home"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Background="Transparent" Margin="24">
                        <StackPanel>
                            <TextBlock Text="About StackSuite" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,8"/>
                            <TreeView x:Name="HomeTreeView" Background="Transparent" Foreground="White">
                                <TreeViewItem Header="📘 Version: 1.0.0.0 – Read Me">
                                    <TreeViewItem Header="View full project readme and version history"
                      MouseLeftButtonUp="TreeViewItem_ReadMe_Selected" />
                                </TreeViewItem>

                                <TreeViewItem Header="🌐 Network Tester">
                                    <TreeViewItem Header="Scan adapters, subnets, IP ranges, or host lists"
                      MouseLeftButtonUp="NetworkTestingTreeViewItem_MouseLeftButtonUp" />
                                </TreeViewItem>

                                <TreeViewItem Header="🔐 SSH Console">
                                    <TreeViewItem Header="Terminal shell access to reachable SSH hosts"
                      MouseLeftButtonUp="SshTreeViewItem_MouseLeftButtonUp" />
                                </TreeViewItem>

                                <TreeViewItem Header="📁 SFTP Client">
                                    <TreeViewItem Header="Graphical file upload/download over SSH"
                      MouseLeftButtonUp="SftpTreeViewItem_MouseLeftButtonUp" />
                                </TreeViewItem>
                            </TreeView>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- Network Tester Tab -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Lan" Width="20" Height="20" Margin="0,0,4,0"/>
                            <TextBlock Text="Network Tester"/>
                        </StackPanel>
                    </TabItem.Header>
                    <DockPanel Background="Transparent">
                        <!-- Toolbar -->
                        <StackPanel Orientation="Horizontal" Margin="16,12" DockPanel.Dock="Top">
                            <TextBox x:Name="NetTesterHostIP" Width="360" Margin="0,0,8,0" FontSize="16" VerticalContentAlignment="Center" materialDesign:HintAssist.Hint="Enter IP or Range"/>
                            <Button x:Name="ScanButton" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0" Padding="12,8" Click="ScanButton_Click" Height="38" Width="102">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Magnify" Width="24" Height="24"/>
                                    <TextBlock Text="Scan" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="CancelButton" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0" Padding="12,8" Click="CancelButton_Click" IsEnabled="False" Width="110" Height="38">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Cancel" Width="24" Height="24"/>
                                    <TextBlock Text="Cancel" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="ImportButton"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,8,0"
                            Padding="12,8"
                            Click="ImportButton_Click"
                            materialDesign:HintAssist.Hint="Load IPs from file"
                            Height="38" Width="110">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileImport" Width="24" Height="24"/>
                                    <TextBlock Text="Import" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="ExportButton"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="0,0,8,0"
                                    Padding="12,8"
                                    Click="ExportButton_Click"
                                    materialDesign:HintAssist.Hint="Export results to file"
                                    Height="38" Width="110">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileExport" Width="24" Height="24"/>
                                    <TextBlock Text="Export" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <ToggleButton x:Name="ShowOfflineToggle"
                                          Width="40" Height="40"
                                          Margin="0,0,8,0"
                                          Checked="ShowOfflineToggle_Changed"
                                          Unchecked="ShowOfflineToggle_Changed">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton" BasedOn="{StaticResource MaterialDesignFloatingActionButton}">
                                        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                                <materialDesign:PackIcon x:Name="ShowOfflineIcon"
                                                        Width="24" Height="24"
                                                        VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Kind" Value="Eye"/>
                                            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Kind" Value="EyeOff"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </ToggleButton>
                            <ComboBox
                            x:Name="InterfaceComboBox"
                            Width="494"
                            DisplayMemberPath="Name"
                            materialDesign:HintAssist.Hint="Network Interface"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                        </StackPanel>
                        <DataGrid x:Name="NetTesterResults"
                                  KeyDown="NetTesterResults_KeyDown"
          AutoGenerateColumns="True"
          AutoGeneratingColumn="NetTesterResults_AutoGeneratingColumn"
          HeadersVisibility="Column"
          CanUserSortColumns="True"
          CanUserResizeColumns="True"
          IsReadOnly="True"
          Background="Transparent"
          Foreground="White"
          GridLinesVisibility="Horizontal"
          FontSize="14"
          materialDesign:DataGridAssist.CellPadding="8"
          Padding="4"
          ContextMenuOpening="NetTesterRow_ContextMenuOpening">

                            <DataGrid.Resources>
                                <ContextMenu x:Key="HostContextMenu" Placement="MousePoint">
                                    <MenuItem Header="🖥 Open in RDP"
                      Click="OpenInRdp_Click" />
                                    <MenuItem Header="🔐 Open in SSH Console"
                      Click="OpenInSsh_Click"
                      x:Name="SSHMenuItem" />
                                    <MenuItem Header="🌐 Open Web UI (HTTP)"
                      Click="OpenHttp_Click"
                      x:Name="HttpMenuItem" />
                                    <MenuItem Header="📂 Browse via UNC Path"
                      Click="OpenUnc_Click" />
                                </ContextMenu>
                            </DataGrid.Resources>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                    <Setter Property="ContextMenu" Value="{StaticResource HostContextMenu}" />
                                    <EventSetter Event="ContextMenuOpening" Handler="NetTesterRow_ContextMenuOpening" />
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </DockPanel>
                </TabItem>
                <!-- SSH Console Tab -->
                <TabItem Header="🔐 SSH Console">
                    <Grid>
                        <TabControl x:Name="SshSessionTabControl"
                         Background="Transparent"
                         Foreground="White"
                         FontSize="13">
                            <TabItem Header="+ New SSH Session" PreviewMouseDown="NewSshSessionTab_Click">
                                <Grid Margin="10">
                                    <StackPanel Orientation="Horizontal"
            Margin="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top">
                                        <TextBox x:Name="SshHostInput" materialDesign:HintAssist.Hint="Host/IP"
             Margin="0,0,8,0"
             Width="160"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                                        <TextBox x:Name="SshUsernameInput" materialDesign:HintAssist.Hint="Username"
             Margin="0,0,8,0"
             Width="140"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                                        <PasswordBox materialDesign:HintAssist.Hint="Password"
                 Margin="0,0,8,0"
                 Width="140"
                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                                        <Button Content="Connect"
            Width="100"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Click="ConnectSshSession_Click" />
                                    </StackPanel>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </TabItem>

                <!-- SFTP Client Tab -->
                <TabItem Header="📁 SFTP Client">
                    <Grid>
                        <TabControl x:Name="SftpSessionTabControl"
                         Background="Transparent"
                         Foreground="White"
                         FontSize="13">
                            <TabItem Header="+ New SFTP Session" PreviewMouseDown="NewSftpSessionTab_Click">
                                <Grid Margin="10">
                                    <StackPanel Orientation="Horizontal"
            Margin="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top">
                                        <TextBox x:Name="SftpHostInput" materialDesign:HintAssist.Hint="Host/IP"
             Margin="0,0,8,0"
             Width="160"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                                        <TextBox x:Name="SftpUsernameInput" materialDesign:HintAssist.Hint="Username"
             Margin="0,0,8,0"
             Width="140"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                                        <PasswordBox materialDesign:HintAssist.Hint="Password"
                 Margin="0,0,8,0"
                 Width="140"
                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                                        <Button Content="Connect"
            Width="100"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Click="ConnectSftpSession_Click" />
                                    </StackPanel>

                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </TabItem>
            </TabControl>
        </DockPanel>
    </Border>
    <Window.Resources>
        <DataTemplate x:Key="ClosableTabHeaderTemplate">
            <DockPanel LastChildFill="False" VerticalAlignment="Center">
                <TextBlock Text="{Binding}" 
                   VerticalAlignment="Center" 
                   Margin="0,0,6,0"
                   Foreground="White" 
                   FontWeight="SemiBold" />

                <Button Width="20" Height="20"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                ToolTip="Close"
                Click="CloseTab_Click"
                VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="White"/>
                </Button>
            </DockPanel>
        </DataTemplate>
    </Window.Resources>
</Window>